import React from 'react'

const ListingItem = ({listing}) => {
  return (
    <>
    <div>{listing.name}</div>
    <div>{listing.img}</div>
    <div>{listing.name}</div>
    <div>{listing.name}</div>

    </>
  )
}

export default ListingItem